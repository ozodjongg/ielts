"use client";import {useEffect,useState} from "react";import {api,portalPath} from "@/lib/api";import {useAuth} from "@/components/auth-provider";import {Card,PageHeader,Loading} from "@/components/ui";
export default function Page(){const a=useAuth(),[centers,setCenters]=useState<any[]>([]),[ov,setOv]=useState<any>(null);useEffect(()=>{if(!a.session)return;Promise.all([api<any>(portalPath("admin","tenant","/centers"),a.session.access_token),api<any>(portalPath("admin","analytics","/overview"),a.session.access_token)]).then(([c,o])=>{setCenters(c.items||[]);setOv(o)}).catch(()=>{})},[a.session]);if(!a.session)return null;return <><PageHeader title="Platform overview" subtitle="Centers, service activity and operational status."/><div className="grid grid-4 section"><Card><div className="muted">Centers</div><div className="metric">{centers.length}</div></Card><Card><div className="muted">Active centers</div><div className="metric">{ov?.active_centers??0}</div></Card><Card><div className="muted">Active students</div><div className="metric">{ov?.active_students??0}</div></Card><Card><div className="muted">Events this month</div><div className="metric">{ov?.events_this_month??0}</div></Card></div><div className="grid grid-2 section"><Card><h3>Service activity</h3>{ov?<div className="stack">{Object.entries(ov.service_activity||{}).map(([k,v])=><div key={k} className="row-between"><span>{k}</span><b>{String(v)}</b></div>)}</div>:<Loading/>}</Card><Card><h3>Security model</h3><div className="stack muted"><span>• Separate admin / center / student origins</span><span>• JWT verified only at gateway</span><span>• HMAC-signed internal identity</span><span>• Schema-separated module data</span><span>• Tenant quotas enforced transactionally</span></div></Card></div></>}
